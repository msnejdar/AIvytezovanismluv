{
  "buildCommand": "cd AI-SEARCH && npm ci && npm run build",
  "outputDirectory": "AI-SEARCH/dist",
  "installCommand": "cd AI-SEARCH && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "AI-SEARCH/api/search.js": {
      "runtime": "nodejs18.x"
    }
  },
  "env": {
    "VITE_CLAUDE_API_KEY": "@vite_claude_api_key"
  },
  "build": {
    "env": {
      "VITE_CLAUDE_API_KEY": "@vite_claude_api_key"
    }
  },
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Access-Control-Allow-Origin",
          "value": "*"
        },
        {
          "key": "Access-Control-Allow-Methods",
          "value": "GET, POST, PUT, DELETE, OPTIONS"
        },
        {
          "key": "Access-Control-Allow-Headers",
          "value": "Content-Type, Authorization"
        }
      ]
    }
  ]
}